# Quickstart

This page will become the main “first run” guide for the package.

## CLI

```bash
jaxparamopt --help
```

## Python

The Python API is still provisional. As the workflow and backend layers stabilize, this page will show the supported programmatic entrypoints and the minimum required configuration objects.

## Current Focus

The current refactor is establishing:

- normalized configuration handling
- backend-aware input loading
- stable workflow staging
- a cleaner separation between orchestration and forcefield-specific logic

# Python API

The Python API is still provisional.

## Intended Direction

The long-term goal is to support:

- importable configuration objects
- reusable workflow entrypoints
- backend-aware input loading
- optimization orchestration from other Python programs

## Current Guidance

Use the CLI as the primary interface unless you are working directly with the development version of the codebase and understand that internal interfaces may change during the refactor.

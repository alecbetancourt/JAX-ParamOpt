# Architecture

High-level architecture planning currently lives in `DESIGN.md` in the repository root.

## Long-Term Goal

The docs site should eventually summarize the stable architectural decisions from that design document without duplicating every working note.

## Current Refactor Focus

- backend segmentation
- workflow staging
- input pipeline cleanup
- clearer public interface boundaries

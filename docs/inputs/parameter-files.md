# Parameter Files

Parameter inputs define which forcefield terms are exposed to the optimizer and how their bounds are interpreted.

## Topics To Document

- parameter file syntax
- grouped parameters
- single-forcefield vs multi-forcefield mappings
- sensitivity and bound handling
- backend-specific parameter selector translation

## Status

The parameter-loading path exists today, but its public format description still needs to be written down explicitly for stable user documentation.

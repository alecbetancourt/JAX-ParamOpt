# CLI

The command-line interface is currently the primary supported user interface.

## Current Entry Point

```bash
jaxparamopt --help
```

## Documentation Scope

This page will eventually document:

- argument groups
- backend-specific options
- input/output conventions
- common CLI workflows
- troubleshooting for runtime configuration

## Status

The CLI surface is still being normalized as configuration and workflow management are extracted from `driver.py`.
